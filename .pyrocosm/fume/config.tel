tel 1.0

# The test assembly (`./mill pyrocosm.test.assembly`), whose suites fume discovers from the
# jar's META-INF/services/probably.Suite index. Relative entries resolve against the invocation
# directory, so run fume from the repository root (`make test`).
classpath out/pyrocosm/test/assembly.dest/out.jar
